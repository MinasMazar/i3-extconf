# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $bcolor #044440
set $fcolor #DD6633

set $term_exec exec i3-sensible-terminal -e

set $ws_1     1: main
set $ws_2     2: ogame
set $ws_3    30: www
set $ws_4    40: ♫
set $ws_5    50: ➤
set $ws_6    60: work
set $ws_7    70: chat
set $ws_8    80: games
set $ws_9    90: i3/system
set $ws_10  100: file
set $outpad 1001

set $ws_main   1: main
set $ws_ogame  2: ogame
set $ws_games 80: games
set $ws_www   30: www
set $ws_media 40: media
set $ws_fm   100: file

set $i3rb exec rvm default do i3rb

#set $Lock exec xscreensaver-command --activate
set $Lock exec i3lock -c "#DD6633" -e
set $Outpad focus output left, workspace "OUTPAD L", focus output right, workspace "OUTPAD R" 
set $Scrot exec scrot 'lock_n_scrot.png'; $Lock

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
                     
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
#font pango:DejaVu Sans Mono 8
font pango:FontAwesome 8, Terminus 8

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
workspace_auto_back_and_forth yes
focus_follows_mouse yes

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Shift+Return exec i3-sensible-terminal -e pry

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu='dmenu -b -l 12'

# Bind Multimedia keys
bindsym XF86AudioPlay               exec xmms2 toggle
bindsym XF86AudioStop               exec xmms2 stop
bindsym XF86AudioPrev               exec xmms2 prev
bindsym XF86AudioNext               exec xmms2 next
bindsym XF86AudioMute               exec amixer -q set Master toggle
bindsym XF86AudioLowerVolume        exec amixer -q set Master 2%- unmute
bindsym XF86AudioRaiseVolume        exec amixer -q set Master 2%+ unmute
bindsym Shift+XF86AudioLowerVolume  exec amixer -q set Master mute
bindsym Shift+XF86AudioRaiseVolume  exec amixer -q set Master toggle

# Bind lockers & shooters
exec xscreensaver -no-splash

bindsym $mod+ugrave          $Outpad, $Lock
bindsym $mod+backslash       $Outpad
bindsym $mod+Shift+ugrave    $Outpad, $Lock
bindsym $mod+Shift+agrave    $term_exec rvm default exec clockwork ~/.i3/xscreensaver_deactivator.rb

bindsym Print                exec xfce4-screenshooter

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym e quit
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Pause mode "$mode_system"

# Bind util X11 bins
bindsym Shift+Pause          $term_exec xev

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
#bindsym $mod+f fullscreen
bindsym $mod+Tab fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+q focus child

# Scratchpadding
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

bindsym $mod+Next workspace next
bindsym $mod+Prior  workspace prev
bindsym $mod+period workspace next
bindsym $mod+comma  workspace prev

bindsym $mod+Shift+Next move container to workspace next, workspace next
bindsym $mod+Shift+Prior  move container to workspace prev, workspace prev

# switch to workspace
bindsym $mod+1 workspace number $ws_1
bindsym $mod+2 workspace number $ws_2
bindsym $mod+3 workspace number $ws_3
bindsym $mod+4 workspace number $ws_4
bindsym $mod+5 workspace number $ws_5
bindsym $mod+6 workspace number $ws_6
bindsym $mod+7 workspace number $ws_7
bindsym $mod+8 workspace number $ws_8
bindsym $mod+9 workspace number $ws_9
bindsym $mod+0 workspace number $ws_10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws_1
bindsym $mod+Shift+2 move container to workspace number $ws_2
bindsym $mod+Shift+3 move container to workspace number $ws_3
bindsym $mod+Shift+4 move container to workspace number $ws_4
bindsym $mod+Shift+5 move container to workspace number $ws_5
bindsym $mod+Shift+6 move container to workspace number $ws_6
bindsym $mod+Shift+7 move container to workspace number $ws_7
bindsym $mod+Shift+8 move container to workspace number $ws_8
bindsym $mod+Shift+9 move container to workspace number $ws_9
bindsym $mod+Shift+0 move container to workspace number $ws_10

# switch to workspace selected by dmenu
bindsym $mod+igrave $i3rb goto_workspace _dmenu_ws_
#bindsym $mod+igrave exec i3-msg workspace `dmenu`

# move focused container to workspace selected by dmenu
bindsym $mod+Shift+igrave $i3rb move_and_goto_workspace _dmenu_ws_

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Escape exec killall i3-nagbar

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

mode "LAUNCH" {
  bindsym f workspace number $ws_10, $term_exec vifm, mode "default"
  bindsym Shift+f workspace number $ws_10, exec thunar, mode "default"
  bindsym o exec firefox, mode "default"
  bindsym r exec chromium-browser, mode "default"
  bindsym k exec gkrellm, mode "default"
  bindsym j exec jedit, mode "default"
  bindsym e $term_exec vim, mode "default"
  bindsym Shift+e exec gvim, mode "default"
  bindsym t exec totem, mode "default"
  bindsym v $term_exec gvim, mode "default"
  bindsym m $term_exec rvm default do tmuxinator start `dmenu -b`, mode "default"

  bindsym Shift+m workspace number "100: file", $term_exec mc, mode "default"
  bindsym x $term_exec alsamixer, mode "default"
  bindsym c workspace $ws_9, exec $term_exec vim ~/.i3/config, mode "default"
  bindsym Shift+c workspace $ws_9, exec gvim ~/.i3/config, mode "default"

  bindsym b $term_exec rvm default do bifrost pry, mode "default"

  bindsym s exec i3-sensible-terminal -e rvm default exec skynet pry `dmenu -b` --config .skynet-config.yaml, mode "default"

  bindsym Shift+s exec i3-sensible-terminal -e rvm default exec skynet rattack 11 `dmenu -b` --config .skynet-config.yaml, mode "default"

  bindsym y exec i3-sensible-terminal -e rvm default exec bifrost ytdl `dmenu -b`, mode "default"

  bindsym F7  $term_exec rvm default exec cappuccino, $term_exec rvm default exec cappuccino, mode "default"

  bindsym F6  exec i3-sensible-terminal -e rvm default exec skynet rattack 9 S126_IT --config .skynet-config.yaml, exec i3-sensible-terminal -e rvm default exec skynet rattack 9 S114_IT --config .skynet-config.yaml, exec i3-sensible-terminal -e rvm default exec skynet rattack 9 S133_IT --config .skynet-config.yaml, mode "default"

  bindsym F3  exec opera, exec chromium-browser, mode "default"
  bindsym F4  workspace $ws_4, layout splitv, $term_exec alsamixer, $term_exec cmus ,  mode "default"

  bindsym F11 $term_exec rvm default do skynet rattack 11 S126_IT --config ~/.skynet-config.yaml, $term_exec rvm default do skynet rattack 11 S133_IT --config ~/.skynet-config.yaml

  bindsym F12 exec xrandr --auto, exec  sleep 1 && xrandr --output VGA1 --right-of LVDS1, mode "default"

  bindsym Escape mode "default"
  bindsym $mod+m mode "default"
}

mode "i3rb" {
  bindsym i $term_exec rvm default do i3rb pry, mode "default"
  bindsym w exec i3-msg workspace `dmenu -b`, mode "default"

  bindsym h exec xmessage workspace handler, mode "default"
  bindsym Escape mode "default"
  bindsym $mod+i mode "default"
}

bindsym $mod+i    mode "i3rb"
bindsym $mod+r    mode "resize"
bindsym $mod+m    mode "LAUNCH"

bindsym $mod+apostrophe workspace back_and_forth
bindsym $mod+Shift+apostrophe move container to workspace back_and_forth
bindsym $mod+Shift+m exec xfce4-appfinder

# Floating assignments
#for_window [title=".*float-terminal.*"] floating enable
#for_window [class="i3k"] floating enable
for_window [class="Xfce4-notifyd"] floating enable
#for_window [class="Gkrellm"] floating enable

# Workspace assignments
assign [class="Audacious"] $ws_media
assign [class=".*cmus.*"] $ws_media
assign [class="Gimp"] gimp
assign [class=".*Opera.*"] $ws_ogame
assign [class=".*Chromium-browser.*"] $ws_www
assign [class="gvim" title=".*i3.*config"] $ws_config
assign [title="i3rb"] $ws_config
 
# Defining borders
new_window pixel 2

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position top
        output LVDS1
        output VGA1
        tray_output LVDS1

        workspace_buttons yes

        # There are several status_command solutions...

        #status_command i3status
        status_command rvm default do ruby ~/.i3/i3ba.rb 2>~/.i3/i3ba.rb.log
        #status_command bash ~/workspace/i3rb/tmp/i3custom_status.sh
        #status_command i3blocks

        font pango:FontAwesome 8, Terminus 8
        colors {
          background $bcolor
          statusline #ffffff
          separator #111111
  
          focused_workspace  $bcolor $bcolor $fcolor
          active_workspace   $bcolor $bcolor #FFFFFF
          inactive_workspace $bcolor $bcolor #FFFFFF
          urgent_workspace   #2f343a #900000 #ffffff
        }

        # Syntax
        # mode <dock|hide|invisible>
        # hidden_state <hide|show>
        # modifier <Modifier>
        mode dock
        hidden_state hide
        modifier Mod4
}


# SETTING COLORS (from http://i3wm.org/docs/userguide.html#_the_tree_consists_of_containers)
# class                 border  backgr. text    indicator

client.focused            $fcolor $fcolor $bcolor $bcolor
client.focused_inactive   $bcolor $bcolor $fcolor $bcolor
client.unfocused          $bcolor $bcolor $fcolor $bcolor
client.urgent             #FF0000 #FF0000 #FFFF00 $bcolor
# client.placeholder      #000000 #0c0c0c #ffffff #000000

exec --no-startup-id nm-applet
exec --no-startup-id pasystray
exec xrandr --output VGA1 --right-of LVDS1
exec_always cd .i3; rvm default exec ruby ~/.i3/graffiti.rb

